import * as React from "react";
import { AllProps, ISize } from "../types";
import { ISpaceContext } from "../ISpaceContext";
export declare const useSpace: (props: AllProps, spaceElement: React.MutableRefObject<HTMLElement | undefined>) => {
    space: import("../types").ISpace;
    parentContext: ISpaceContext;
    currentContext: ISpaceContext;
    currentSize: ISize;
    resizing: boolean;
};
